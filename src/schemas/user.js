const UserType = `
  # A user in our app
  type User {
    _id: String,
    firstName: String,
    lastName: String,
    email: String,
    age: Int
    company: Company
  }
`;

export default UserType;
